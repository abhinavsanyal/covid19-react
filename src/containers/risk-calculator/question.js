import React , { useState, useEffect} from 'react';

export default props => {
    
     const {question,onAnswer} = props;
      return (
        <React.Fragment>
         <div> this is the question component</div>
        </React.Fragment>
      );

}
